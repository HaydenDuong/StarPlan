<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StarPlan" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StarPlan" parent="Base.Theme.StarPlan">
        <item name="android:windowBackground">@drawable/bg_splash_full</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="android:windowSplashScreenBackground">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.StarPlan.App</item>
    </style>

    <style name="Theme.StarPlan.App" parent="Base.Theme.StarPlan">
        <item name="android:windowBackground">@drawable/bg_splash_full</item>
    </style>
</resources>